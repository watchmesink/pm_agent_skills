# Monitor AI Coding Competitors

Use this skill for a stateful, evidence-first weekly scan of AI coding tools and adjacent prompt-to-app builders. It checks official releases first, samples public Reddit/X/news signals, preserves per-source checkpoints, and produces normalized findings, coverage metadata, and a dated product-update dashboard.

The visible HTML output is intentionally focused: it groups every distinct in-window version or material event beneath each product, shows announcement dates and factual bullets, and omits families with no surfaced content. Use the Markdown report and machine-readable run artifacts when the analytical sections or complete coverage inventory are needed.
